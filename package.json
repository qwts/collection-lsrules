{
  "name": "collection-lsrules",
  "version": "1.0.0",
  "description": "Little Snitch rule generator for coding agents",
  "main": "generate-rules.js",
  "scripts": {
    "generate": "node scripts/generate-rules.js",
    "generate:browsers": "node scripts/generate-rules.js browsers",
    "generate:coding": "node scripts/generate-rules.js coding",
    "generate:terminal": "node scripts/generate-rules.js terminal",
    "check": "node scripts/generate-rules.js --check",
    "extract": "node scripts/generate-rules.js extract",
    "format-remotes": "node scripts/format-remotes.js --format",
    "extract-remotes": "node scripts/format-remotes.js --extract",
    "merge-remotes": "node scripts/format-remotes.js --merge",
    "test": "node scripts/generate-rules.js --check"
  },
  "keywords": [
    "little-snitch",
    "rules",
    "firewall",
    "coding-agents"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}